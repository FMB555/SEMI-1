import React from 'react'

export default function CardCampo(props) {

    /*const campos = props.campos.map(campo => {
        
    })*/

    return (
        <div className={}>
            
        </div>
    )
}
